/**
 * dsh-plugin-autoresearch — client face (web bundle).
 *
 * M1 stub: a valid closure-factory module registering nothing yet. The M2
 * dashboard (sidebar tab, toolviews, badges, overlay fallback) builds on the
 * client-ui slots and the /autoresearch/* HTTP API exposed by the host face.
 *
 * IMPORTANT: only the frozen external module table (react, react-dom,
 * @deepseek-ai/cordis, @deepseek-ai/dsh-client-*) may be imported here — the
 * tsdown build enforces the purity gate.
 */

export {};
